[package]
name = "rex-web"
version = "0.1.0"
edition = "2018"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rex = { path = "..", default-features = false }
wasm-bindgen = "0.2"
